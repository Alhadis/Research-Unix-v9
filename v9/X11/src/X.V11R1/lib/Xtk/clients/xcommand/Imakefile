       INCLUDES = -I$(OLDXRMSRC) -I$(TOP) -I$(TOP)/X11
           OBJS = xcommand.o
           SRCS = xcommand.c

SingleProgramTarget(xcommand,$(OBJS),$(XTOOLLIB) $(OLDXRMLIB) $(XLIB),)
DependTarget()
